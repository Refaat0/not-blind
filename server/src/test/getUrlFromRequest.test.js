// Author: Refaat
import getUrlFromRequest from "../utilities/getUrlFromRequest.js";
import assert from "node:assert";


describe("utilities.getUrlFromRequest.js", () => {
    it("checks if 2 > 1", () => {
        assert(2 > 1, "Two must be greater than one");
    });
});
